# FlowHub 环境变量示例
# 个人 MVP 默认从前端设置页把 key 存到 localStorage，运行时由 /api/chat/* Route Handler 转发。
# 这些服务端 env 是可选的：如果你不想用前端 localStorage 存 key，可以在这里填，
# Route Handler 会优先读 env，没有再读请求体里带过来的 key。

MOONSHOT_API_KEY=
DEEPSEEK_API_KEY=
ANTHROPIC_API_KEY=

# 端点（默认值已写在代码里，按需覆盖）
MOONSHOT_BASE_URL=https://api.moonshot.cn/v1
DEEPSEEK_BASE_URL=https://api.deepseek.com/v1
